<%= form_with(model: theme, url: link, local: true, class: 'content-section-wrapper admin') do |form| %>

  <% if theme.errors.any? %>
    <div class="input-wrapper">
      <div class="error_explanation">
        <h2><%= pluralize(theme.errors.count, "error") %> prohibited this theme from being saved:</h2>

        <ul>
          <% theme.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="input-wrapper">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="input-wrapper">
    <%= form.submit 'Submit', class: "btn --submit" %>
  </div>

<% end %>