<div class="content-wrapper">

  <%= render partial: "layouts/shared/breadcrumbs", locals: { linkText: @title } %>

  <%= render 'form', product: @product, link: admin_product_path(@product) %>

  <div class="content-section-wrapper">
    <%= link_to 'Show', admin_product_path(@product.id), class: "btn" %>
    <%= link_to 'Back', admin_products_path, class: "btn" %>
  </div>

</div>