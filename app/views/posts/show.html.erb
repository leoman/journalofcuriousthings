<div class="container flex post">

  <div class="w-4/5">
  
    <div class="title">
      <h3><%= @post.title %></h3>
      <h4><%= @post.subtitle %></h4>
    </div>

    <div class="image">
      <% if @post.mainImage.attached? %>
        <img src="<%= (url_for(@post.mainImage)) %>" />
      <% end %>
    </div>
  </div>

  <div class="">
    side
  </div>

</div>